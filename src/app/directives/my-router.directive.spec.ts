import { MyRouterDirective } from './my-router.directive';

describe('MyRouterDirective', () => {
  it('should create an instance', () => {
    const directive = new MyRouterDirective();
    expect(directive).toBeTruthy();
  });
});
