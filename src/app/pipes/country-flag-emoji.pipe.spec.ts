import { CountryFlagEmojiPipe } from './country-flag-emoji.pipe';

describe('CountryFlagEmojiPipe', () => {
  it('create an instance', () => {
    const pipe = new CountryFlagEmojiPipe();
    expect(pipe).toBeTruthy();
  });
});
